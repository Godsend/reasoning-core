# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# NotebookLM state (contains a notebook id, not sensitive, but project-local)
references/nblm_notebook_state.json

# Large generated media artifacts (audio overviews) — not committed to git;
# reproduce via reasoning_core_nblm.py synth + poll_synthesis.py
references/*.m4a
references/*.mp3

# OS
.DS_Store
Thumbs.db
